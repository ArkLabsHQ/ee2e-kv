node_modules/
dist/
*.tsbuildinfo
.enclave/
result
result-*
server/public/
.DS_Store
*.log
.env
.env.local

# Dev-only artefacts produced by the Makefile (HMAC keys, runtime tokens, log
# pipe directory). Never commit these — they are regenerated on demand.
.dev-*
