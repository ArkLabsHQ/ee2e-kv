{
  "name": "enclave-e2ee-kv",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "workspaces": [
    "protocol",
    "lib",
    "server",
    "client",
    "test/browser"
  ],
  "scripts": {
    "build": "npm -w @e2ee-kv/protocol run build && npm -w @e2ee-kv/sdk run build && npm -w e2ee-kv-server run build && npm -w e2ee-kv-client run build",
    "test": "npm -w @e2ee-kv/protocol test --if-present && npm -w @e2ee-kv/sdk test --if-present && npm -w e2ee-kv-server test --if-present && npm -w e2ee-kv-client test --if-present",
    "typecheck": "tsc -b protocol lib server client",
    "dev:server": "npm -w e2ee-kv-server run dev",
    "dev:client": "npm -w e2ee-kv-client run dev",
    "clean": "rm -rf protocol/dist lib/dist server/dist client/dist"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "playwright": "^1.49.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  },
  "overrides": {
    "esbuild": "^0.25.0"
  }
}
